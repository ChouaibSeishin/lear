/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { StepRequest } from '../../models/step-request';
import { StepResponse } from '../../models/step-response';

export interface CreateStep$Params {
      body: StepRequest
}

export function createStep(http: HttpClient, rootUrl: string, params: CreateStep$Params, context?: HttpContext): Observable<StrictHttpResponse<StepResponse>> {
  const rb = new RequestBuilder(rootUrl, createStep.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<StepResponse>;
    })
  );
}

createStep.PATH = '/api/steps';
